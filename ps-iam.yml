management:
  tracing:
    sampling:
      probability: 1.0
    propagation:
      type: W3C

  otlp:
    tracing:
      endpoint: http://${JAEGER_HOST:jaeger}:4318/v1/traces
    metrics:
      export:
        enabled: false

  endpoints:
    web:
      exposure:
        include: refresh
